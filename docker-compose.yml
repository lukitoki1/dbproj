version: "3.7"

services:
  postgres:
    container_name: postgres_con
    build: ./postgres
    ports:
      - "5432:5432"

  django:
    container_name: django_con
    build: ./django
    ports:
      - "8000:8000"
    depends_on:
      - postgres